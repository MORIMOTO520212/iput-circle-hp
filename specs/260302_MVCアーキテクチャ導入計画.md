MVC アーキテクチャ導入計画 │
│ │
│ Context │
│ │
│ functions.php が 1,644 行・関数 35 個のモノリシック構造になっており、認証・投稿・Discord・メールの処理が混在して │
│ いる。各テンプレートファイルにもビジネスロジックが混在（post-circle.php 冒頭 164 行など）。Qiita 記事の MVC │
│ パターンを参考に、WordPress テーマの制約（Composer なし・PSR-4 オートロードなし）の中で段階的に分割する。 │
│ │
│ 推奨ディレクトリ構造 │
│ │
│ iput-circle-hp/ │
│ ├── functions.php ← 移行後は 7 行のローダーのみ │
│ ├── routes.php ← ApiRouter クラスに置き換え │
│ │ │
│ ├── public/ │
│ │ ├── image/ ← 【移動先】現 src/ の画像を全移動 │
│ │ │ ├── no_image.png │
│ │ │ ├── iputone_logo.svg │
│ │ │ └── ...（全画像） │
│ │ └── src/ ← 既存の React/TypeScript ソース（変更なし） │
│ │ ├── components/ │
│ │ ├── pages/ │
│ │ └── ... │
│ │ │
│ └── src/ ← PHP クラス置き場（画像移動後に整備） │
│ ├── bootstrap.php ← spl_autoload_register + Hook 登録の起点 │
│ ├── models/ │
│ │ ├── CircleModel.php ← wp_insert_post / get_post_meta など DB 操作 │
│ │ ├── ActivityModel.php │
│ │ ├── NewsModel.php │
│ │ ├── UserModel.php ← signups テーブル・wp_usermeta 操作 │
│ │ └── ImageModel.php ← upload_image / get_attachment_id_from_src │
│ ├── controllers/ │
│ │ ├── CircleController.php ← POST ハンドリング + prepareView() │
│ │ ├── ActivityController.php │
│ │ ├── NewsController.php │
│ │ ├── AuthController.php ← ログイン・サインアップ・プロフィール │
│ │ └── DiscordController.php ← OAuth + 参加申請通知 │
│ ├── services/ │
│ │ ├── MailService.php ← my_sendmail / my_gas_sendmail │
│ │ ├── DiscordService.php ← Discord API HTTP 通信 │
│ │ └── ValidationService.php ← 入力バリデーション共通化 │
│ ├── hooks/ │
│ │ ├── PostTypeHook.php ← add_action('init', ...) 系をまとめる │
│ │ ├── AdminHook.php ← add_action('admin_menu', ...) 系 │
│ │ └── RequestHook.php ← after_setup_theme での POST dispatch │
│ └── helpers/ │
│ ├── UrlHelper.php ← page_url クラス │
│ └── ViewHelper.php ← modal() などの View ユーティリティ │
│ │
│ 各層の責務 │
│ │
│ ┌─────────────────────┬───────────────────────────────────────────────────────────────────┬───────────────────┐ │
│ │ 層 │ 責務 │ 禁止事項 │ │
│ ├─────────────────────┼───────────────────────────────────────────────────────────────────┼───────────────────┤ │
│ │ Model │ WP_Query・wp_insert_post・update_post_meta などの DB 操作のみ │ wp_redirect・echo │ │
│ ├─────────────────────┼───────────────────────────────────────────────────────────────────┼───────────────────┤ │
│ │ Controller │ $_POST 読み取り・バリデーション委譲・Model                        │ 直接 DB 操作      │   │
│ │                     │ 呼び出し・リダイレクト。View 用データを配列で返す                 │                   │   │
│ ├─────────────────────┼───────────────────────────────────────────────────────────────────┼───────────────────┤   │
│ │ Service             │ メール・Discord API などの外部 IO                                 │ WP                │   │
│ │                     │                                                                   │ テンプレート依存  │   │
│ ├─────────────────────┼───────────────────────────────────────────────────────────────────┼───────────────────┤   │
│ │ Hook                │ add_action / add_filter の登録のみ                                │ ビジネスロジック  │   │
│ ├─────────────────────┼───────────────────────────────────────────────────────────────────┼───────────────────┤   │
│ │ View（.php          │ $view_data を受け取って描画するだけ                               │ POST 処理・DB     │   │
│ │ テンプレート）      │                                                                   │ 操作              │   │
│ └─────────────────────┴───────────────────────────────────────────────────────────────────┴───────────────────┘   │
│                                                                                                                   │
│ functions.php の最終形                                                                                            │
│                                                                                                                   │
│ <?php                                                                                                             │
│ require_once(ABSPATH . 'wp-admin/includes/image.php');                                                            │
│ require_once(ABSPATH . 'wp-admin/includes/file.php');                                                             │
│ require_once(ABSPATH . 'wp-admin/includes/media.php');                                                            │
│ require_once(ABSPATH . 'wp-admin/includes/taxonomy.php');                                                         │
│ require_once(ABSPATH . 'wp-includes/ms-functions.php');                                                           │
│ require_once(__DIR__ . '/src/bootstrap.php');                                                                     │
│                                                                                                                   │
│ テンプレートファイル（View）の呼び出しパターン                                                                    │
│                                                                                                                   │
│ // post-circle.php（移行後）                                                                                      │
│ <?php                                                                                                             │
│ $controller = new CircleController();                                                                             │
│ $view_data  = $controller->prepareView(get_params('_post'), (int) get_params('id'));                              │
│ // controller 内で認可失敗時は wp_redirect() + exit 済み                                                          │
│ get_header();                                                                                                     │
│ ?>                                                                                                                │
│ <!-- 以降は $view_data を使うだけの HTML -->                                                                      │
│                                                                                                                   │
│ 移行ステップ（段階的・各フェーズで動作確認する）                                                                  │
│                                                                                                                   │
│ Phase 0 — 画像を public/image/ に移動（先決）                                                                     │
│                                                                                                                   │
│ 1. public/image/ ディレクトリを作成                                                                               │
│ 2. 現 src/ の画像ファイル（PNG・SVG・JPG・WebP）をすべて public/image/ に移動                                     │
│ 3. PHP テンプレート全ファイルの get_theme_file_uri('src/...') を get_theme_file_uri('public/image/...')           │
│ に一括置換                                                                                                        │
│   - 対象ファイル（16 箇所以上）: header.php, footer.php, front-page.php, single-circle.php, single.php,           │
│ search-activity.php, search-news.php, faq.php, contact.php, profile.php                                           │
│ 4. CSS/JS ファイル内の src/ 参照も確認・更新                                                                      │
│ 5. 動作確認（全ページの画像表示）                                                                                 │
│                                                                                                                   │
│ Phase 1 — 下準備（動作変化なし）                                                                                  │
│                                                                                                                   │
│ 1. src/helpers/, src/models/, src/controllers/, src/services/, src/hooks/ を作成                                  │
│ 2. src/helpers/ViewHelper.php に modal() をそのままコピー                                                         │
│ 3. src/helpers/UrlHelper.php に page_url クラスと初期化コードを移動                                               │
│ 4. functions.php で 2〜3 を require_once する                                                                     │
│ 5. 動作確認                                                                                                       │
│                                                                                                                   │
│ Phase 2 — Hook 整理（動作変化なし）                                                                               │
│                                                                                                                   │
│ 1. PostTypeHook.php に post_type_circle / not_create_image / otocon_resize_at_upload を移動                       │
│ 2. AdminHook.php に set_custom_fields / form_01~03_custom_fields / save_custom_fields を移動                      │
│ 3. RequestHook.php に after_setup_theme 内の if-elseif ディスパッチを移動                                         │
│ 4. bootstrap.php を作成（spl_autoload_register + 各 Hook の register() 呼び出し）                                 │
│ 5. functions.php を bootstrap.php の require_once だけに縮小                                                      │
│ 6. 動作確認                                                                                                       │
│                                                                                                                   │
│ Phase 3 — Service 層                                                                                              │
│                                                                                                                   │
│ 1. MailService.php に my_sendmail / my_gas_sendmail を移動                                                        │
│ 2. DiscordService.php に Discord API HTTP 通信を抽出                                                              │
│ 3. ValidationService.php で共通バリデーションを整理                                                               │
│ 4. 動作確認（メール・Discord 通知）                                                                               │
│                                                                                                                   │
│ Phase 4 — Model 層                                                                                                │
│                                                                                                                   │
│ 1. UserModel.php（signup_provisional / user_activation の DB 操作）                                               │
│ 2. ImageModel.php（upload_image / get_attachment_id_from_src）                                                    │
│ 3. CircleModel.php（post_circle の DB 操作部分を抽出）                                                            │
│ 4. ActivityModel.php / NewsModel.php                                                                              │
│ 5. 動作確認（各投稿処理）                                                                                         │
│                                                                                                                   │
│ Phase 5 — Controller 層＋テンプレート整理                                                                         │
│                                                                                                                   │
│ 1. AuthController.php（user_login / user_signup / profile_update）                                                │
│ 2. CircleController.php（post_circle の調整ロジック + prepareView()）                                             │
│ 3. ActivityController.php / NewsController.php / DiscordController.php                                            │
│ 4. 各テンプレート冒頭のビジネスロジックを Controller 呼び出しに置き換え                                           │
│ 5. 全機能の動作確認                                                                                               │
│                                                                                                                   │
│ Phase 6 — REST API 整理                                                                                           │
│                                                                                                                   │
│ 1. routes.php を ApiRouter クラスに移行                                                                           │
│ 2. bootstrap.php で (new ApiRouter())->register() を呼ぶ                                                          │
│ 3. 動作確認（REST エンドポイント）                                                                                │
│                                                                                                                   │
│ 注意事項                                                                                                          │
│                                                                                                                   │
│ - グローバル変数（$page_url など）は移行完了まで bootstrap.php に残して互換性を維持する │
│ - 既存の関数（modal() 等）は移行完了まで削除しない（新旧共存で段階的に置き換え） │
│ - discord_authentication() のクライアントシークレットは DiscordService 移行時に get_option() 経由に改善する │
│ │
│ 重要ファイル │
│ │
│ - /Users/yuma/Documents/iput-circle-hp/functions.php — 移行元（1,644 行） │
│ - /Users/yuma/Documents/iput-circle-hp/routes.php — ApiRouter 化の参考実装 │
│ - /Users/yuma/Documents/iput-circle-hp/post-circle.php — Controller 導入の最初の対象 │
│ - /Users/yuma/Documents/iput-circle-hp/profile.php — ロジック量が少なく練習台として最適 │
│ │
│ 検証方法 │
│ │
│ - 各フェーズ完了後にブラウザで全ページを手動確認 │
│ - サークル作成・活動記録投稿・ニュース投稿・Discord 認証・メール送信の動作確認 │
│ - REST API エンドポイント（/wp-json/custom/v2/circle/list）が応答するか確認
